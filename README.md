# 工具集 (Toolset)

> 该项目初始版本版本由AI生成。

一个基于 Vite + React + Tailwind CSS 构建的现代化工具集应用。

## 功能特性

- 🎨 **现代化界面**: 采用 Dark 主题，简洁美观
- 📁 **分类导航**: 左侧显示工具分类，点击分类查看工具列表
- 🔗 **新窗口打开**: 点击工具卡片在新窗口打开工具链接
- 📱 **响应式布局**: 工具卡片采用弹性网格布局，自适应不同屏幕
- ⚙️ **配置化**: 通过 JSON 文件轻松管理工具集
- 🐳 **Docker 支持**: 提供 Dockerfile 和 docker-compose 配置

## 技术栈

- **Vite**: 快速的前端构建工具
- **React**: 用户界面库
- **Tailwind CSS**: 实用优先的 CSS 框架
- **Nginx**: 高性能的静态文件服务器

## 项目结构

```
toolset-app/
├── src/
│   ├── App.jsx          # 主应用组件
│   ├── App.css          # 应用样式
│   ├── main.jsx         # 应用入口
│   ├── index.css        # 全局样式（包含 Tailwind）
│   └── tools.json       # 工具配置文件
├── public/              # 静态资源
├── index.html           # HTML 模板
├── tailwind.config.js   # Tailwind 配置
├── postcss.config.js    # PostCSS 配置
├── nginx.conf           # Nginx 配置文件
├── Dockerfile           # Docker 镜像构建文件
├── docker-compose.yml   # Docker Compose 配置
├── .dockerignore        # Docker 忽略文件
└── package.json         # 项目依赖
```

## 配置工具集

编辑 `src/tools.json` 文件来添加或修改工具：

```json
{
  "categories": [
    {
      "name": "分类名称",
      "tools": [
        {
          "name": "工具名称",
          "url": "工具链接"
        }
      ]
    }
  ]
}
```

## 开发

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 预览生产构建
npm run preview
```

## Docker 部署

### 使用 Docker 构建

```bash
# 构建镜像
docker build -t toolset-app .

# 运行容器
docker run -d -p 8080:80 --name toolset-app toolset-app
```

### 使用 Docker Compose

```bash
# 启动服务
docker-compose up -d

# 停止服务
docker-compose down

# 查看日志
docker-compose logs -f
```

### 访问应用

部署后，在浏览器中访问 `http://localhost:8080`

## 使用说明

1. 左侧导航栏显示所有工具分类
2. 点击分类在右侧显示该分类下的所有工具
3. 工具以卡片形式展示，采用响应式网格布局
4. 点击工具卡片在新窗口打开工具链接

## Docker 配置说明

- **多阶段构建**: 使用 Node.js 构建应用，使用 Nginx 提供静态文件服务
- **镜像优化**: 基于 Alpine Linux，镜像体积小
- **性能优化**: 启用 Gzip 压缩，配置静态资源缓存
- **安全配置**: 添加安全响应头，禁止访问隐藏文件
- **SPA 支持**: 配置 Nginx 支持 React Router
